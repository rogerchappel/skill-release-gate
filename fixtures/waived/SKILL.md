# Waived Skill

Use this skill when an agent needs a local review checklist.

Required input is a folder containing skill documentation.

Tools: shell and filesystem access for local checks.

Example: run the checklist against a candidate folder.

Verification: run the smoke fixture and inspect the release candidate report.

Limitations: cannot judge domain accuracy.

Release notes: initial release candidate fixture.
