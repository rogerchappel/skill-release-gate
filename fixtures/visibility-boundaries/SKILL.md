# Visible boundaries

<!-- hidden activation --> Use this skill when a required input is provided.

```markdown
<!-- hidden comment marker -->
hidden content
```
Use the CLI tool and request approval before any side effect.

~~~text
hidden content
~~~
Example: run a verification test. Limitation: no network. Release notes: initial version.
