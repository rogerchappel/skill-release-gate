# Contributing

Keep changes local-first, deterministic, and fixture-backed.

## Development

```bash
npm test
npm run check
npm run smoke
```

## Adding Checks

Add one focused fixture for the behavior, update `docs/CHECKS.md`, and keep error messages actionable for an agent reviewer.
